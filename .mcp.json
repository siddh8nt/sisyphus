{
  "mcpServers": {
    "sisyphus": {
      "command": "node",
      "args": ["${SISYPHUS_HOME:-.}/mcp/index.js"],
      "env": {
        "SISYPHUS_ORCH": "${SISYPHUS_ORCH:-http://127.0.0.1:4100}"
      }
    }
  }
}
